{
  "name": "openzeppelin-exploration",
  "version": "1.0.0",
  "description": "Personal exploration of OpenZeppelin Contracts — ERC-20, ERC-721, AccessControl",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
